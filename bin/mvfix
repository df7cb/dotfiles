#!/bin/sh

if [ -z "$1" ] ; then
	set *
fi

for f in "$@" ; do
	f2=`echo "$f" | tr ' ™šá„”' '_ÄÖÜßäöü'`
	f2=`echo "$f2" | sed 's/%20/_/g'`
	if [ "$f" = "$f2" ] ; then
		#echo "$f: no change"
		continue
	fi
	mv -iv "$f" "$f2"
done
