# $Id$

set folder="~/mail"

# external files
source ~/.mutt/options
source ~/.mutt/bind
source ~/.mutt/color
source ~/.mutt/headers
source ~/.mutt/x-face
source ~/.mutt/lists
source ~/.mutt/hooks

# aliases
source ~/.mutt/aliases
source `if [ -f ~/.priv/aliases.ab ] ; then echo ~/.priv/aliases.ab ; else echo /dev/null ; fi`

# local configuration options
#source `if [ -f ~/.mutt/muttrc.local ] ; then echo ~/.mutt/muttrc.local ; else echo /dev/null ; fi`

set from="cb@df7cb.de"
set hostname="df7cb.de"
macro generic & "!retch -F\cM"
macro index & "!retch -F\cM"
set spoolfile="=IN/cb"
mailboxes ! =rw/inbox =IN/root =IN/pisg =IN/mutt =IN/debian =IN/lists =IN/tagesschau
color header brightblack default heim-d@df7cb.de
