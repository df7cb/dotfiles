#!/bin/sh
# $Id$

if [ -z "$1" ] ; then
	echo "usage: $0 hostname command" 1>&2
	exit 1
fi
hostname=$1
shift

: ${HOSTDISPLAY:=$DISPLAY}
xkey=`xauth list $HOSTDISPLAY`

ssh -x -f $hostname "xauth add $xkey; DISPLAY=$HOSTDISPLAY $* &"
