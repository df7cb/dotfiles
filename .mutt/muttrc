# $Id$

set folder="~/mail/rw"

# external files
source ~/.mutt/options
source ~/.mutt/bind
source ~/.mutt/color
source ~/.mutt/headers
source ~/.mutt/x-face
source ~/.mutt/lists
source ~/.mutt/hooks

# aliases
source ~/.mutt/aliases
source `if [ -f ~/.priv/aliases.ab ] ; then echo ~/.priv/aliases.ab ; else echo /dev/null ; fi`

# local configuration options
#source `if [ -f ~/.mutt/muttrc.local ] ; then echo ~/.mutt/muttrc.local ; else echo /dev/null ; fi`

# local settings
set folder="~/mail/rw"
mailboxes =inbox

macro	pager	S	"<exit>S"
macro	index	S	"<save-message>=spam-queue/spam<enter>" "Save spam for processing with SA"
macro	index	<ESC>S	"<tag-pattern>!~D<enter><tag-prefix><copy-message>=spam-queue/ham<enter><untag-pattern>.<enter>" "Save ham for processing with SA"

set from="cb@cs.uni-sb.de"
set print_command="enscript"
