#!/bin/sh
# $Id$

if [ $1 ] ; then
	FILE="$1"
elif up d096.stw.stud.uni-saarland.de ; then
	URL="http://www.df7cb.de/bin/random-comic"
	FILE="/tmp/xroot-$$"
	RM=1
	wget -q -O "$FILE" "$URL"
else
	FILE="$HOME/bilder/back"
fi

XV="xv -root -quit +noresetroot -rmode 5 -rbg navy"
[ -f $FILE ] && $XV $FILE

[ $RM ] && rm -f $FILE
