#!/bin/sh
# $Id$

case "$1" in
	*.tar)	tar tfv "$@" ;;
	*.tgz|*.gz|*.tar.Z)	tar tfvz "$@" ;;
	*.bz2)	tar tfvj "$@" ;;
	*.zip)	unzip -l "$@" ;;
	*)	echo "$0 error: suffix not recognized: $1" 1>&2 ;;
esac
