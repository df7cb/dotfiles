#!/bin/sh

case $1 in
	*/.priv/ssh/*)
		DOMAIN=$(basename "$1")
		GNOME_KEYRING_QUERY="ssh_$DOMAIN" SSH_ASKPASS="$HOME/bin/$OS/gnome-keyring-query" \
			/usr/bin/ssh-add "$@" < /dev/null
		;;
	*)
		/usr/bin/ssh-add "$@" ;;
esac
